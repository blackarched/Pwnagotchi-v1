// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';

// You can add other global setup here if needed for your tests.
// For example, mocking global objects or functions:
/*
jest.mock('some-global-module', () => ({
  // ...mock implementation
}));

beforeAll(() => {
  // Runs once before all tests
});

afterEach(() => {
  // Runs after each test, e.g., for cleanup
  // cleanup(); // React Testing Library does this automatically after each test with default config
});

afterAll(() => {
  // Runs once after all tests
});
*/
[end of frontend/src/test/setup.js]
