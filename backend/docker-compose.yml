version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/mesh/peers"]
      interval: 30s
      timeout: 10s
      retries: 5
